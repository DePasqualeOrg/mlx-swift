// Empty translation unit. SwiftPM requires at least one compilable source in
// a target; in the "external Cmlx" mode (MLX_SWIFT_USE_EXTERNAL_CMLX=1) all
// MLX symbols come from prebuilt libraries supplied by a CMake install, and
// this file exists only to satisfy SwiftPM.
